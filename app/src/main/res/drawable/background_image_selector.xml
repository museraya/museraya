<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State when selected -->
    <item android:state_selected="true"
        android:drawable="@drawable/bg_selector_border" />
    <!-- Default state -->
    <item android:drawable="@drawable/bg_selector_no_border" />
</selector>